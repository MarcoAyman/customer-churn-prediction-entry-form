/* PostCSS processes CSS through a plugin chain.
   tailwindcss: generates utility classes from your config.
   autoprefixer: adds vendor prefixes (e.g. -webkit-backdrop-filter)
   so glassmorphism effects work across all modern browsers. */
export default {
  plugins: {
    tailwindcss:  {},
    autoprefixer: {},
  },
}
